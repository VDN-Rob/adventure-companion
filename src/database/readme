Everything related to SQLite persistence and database access.

The database.ts file is meant to initialize the DB and make sure the tables exists.

The repositories in the dataAccessLayer directory are meant to shield the database from the business layer.