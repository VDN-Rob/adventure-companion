Important:
- Change POI details screen to POI edit screen

QOL:
- calendar for dates
- stops sorted by distance from start
- Add custom poitypes
- suggestions if no POI/Days, ...

UI:
- Copy/move editing and deleting inside of trip details
- styling:
    * index
    * tripDetailsScreen
    * createTripScreen
    * editTripScreen
    * createDayScreen
    * dayDetailsScreen
    * create POI Screen
    * edit POI Screen

Bugs:
- possible to go back in index with added trips dissapearing

Validation:
- Add validation in application layer
- start date < end date
- Day date in range trip dates
- basic validation:
    * edit Day
    * create Trip
    * edit Trip
    * create POI
    * edit POI
- Advanced validation:
    * create Day
    * edit Day
    * create Trip
    * edit Trip
    * create POI
    * edit POI

REfactoring:
- Move elements into "cards". example:
DayDetails
│
├── DayHeader
│
├── RouteSummary
│
├── Map
│
├── Planned stops
│
├── Notes
│
├── Photos
│
└── Recorded ride
- Separate errors
Day
──────────────────

120 km
850 m

POIs
──────────────────

⚠ Unable to load POIs
[Retry]


## Full run:
# Phase 1 : Launching application
[UI]  Very empty and dark -> feels weird.

# Phase 2 : adventures
[UX]  If packed schedule -> long scroll to go down.
=> creation should not be allowed unless required fields are filled (see poi/createPoi)
// Step 3
[MIS] tripsScreen: Pressing on upcoming/past adventure does nothing

[UX]  No overlapping dates possible
[UX]  Inline errors for impossible elements
[UX]  Need to reload to update index after creating a trip -> useFocusEffect
[UX]  Dates should be with calendar or fixed input
[UX]  Budget currency should be a selection

[UI]  tripsScreen: When empty, everything is packed at the top -> add a bit more spacing
[UI]  Alert for end date < start date should be a modal


// Step 7
[UX]  Need to reload to update index after deleting an adventure -> useFocusEffect

# Phase 3: Days
=> creation should not be allowed unless required fields are filled (see poi/createPoi)
// Step 9
[UX] Possibility of creating duplicate dates
[UX] Possibility of creating dates outside of date range
[UX]  Dates should be with calendar or fixed input

[UI] Add Day counter on day card in tripDetailsScreen and day/detailsDay (day.date - trip.startdate) - does not seem to work currently
[UI] day/editDay: alert for empty date is generic
[UI] day/editDay: alert for negative distance is generic
[UI] day/editDay: alert for negative elevation is generic

# Phase 4: POIs
// Step 15
[MIS] POIS on map are still example POIs, not day POIs

[UI] Danger zone POI is not as visible as on edit day/trip

// Step 16 - createPoi
[UX]  Invalid Latitude/Longitude just returns, does not raise alert 
[UX]  Dates should be with calendar or fixed input

// Step 18
[UX]  Editing instead of seeing detail of POI => add detailsPOI screen

# Phase 5: Check in
// Step 23
[UX]  Undoing check in should be doable from editPOI

# Phase 6: Finance
=> Not possible to test yet due to MIS
// Step 26
[MIS] Finance screen is not linked to database yet

# Phase 7: Diary
=> Not possible to test yet due to MIS
// Step 33
[MIS] Quick action diary is not linked
[MIS] More menu diary is not linked to adventure
